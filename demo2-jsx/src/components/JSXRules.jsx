const Rules = () => {
    return (
        <div>
            <h1> JSX Rules </h1>
            <p> JSX must return single parent element.</p>
            <p> JSX element must be closed properly.</p>
            <p> JSX attributes written using CameCase. (eg. className)</p>
        </div>
    );
}

export default Rules;